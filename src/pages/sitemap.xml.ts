import type {APIRoute} from "astro";import {contentPages} from "../lib/site-routes";import {resolveSiteConfig} from "../lib/site-config";
export const GET:APIRoute=()=>{const site=resolveSiteConfig(import.meta.env.PUBLIC_SITE_ORIGIN);const origin=site.origin??"https://goaliegearfit.pages.dev";const urls=contentPages.map(page=>`<url><loc>${new URL(page.path,origin)}</loc><lastmod>${page.lastmod}</lastmod></url>`).join("");return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls}</urlset>`,{headers:{"Content-Type":"application/xml"}})};
