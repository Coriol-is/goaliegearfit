import {readFileSync} from "node:fs";import {describe,expect,it} from "vitest";import {contentRoutes} from "../src/lib/site-routes";
describe("SEO route inventory",()=>{it("contains each intended route once",()=>{expect(contentRoutes).toHaveLength(11);expect(new Set(contentRoutes).size).toBe(contentRoutes.length);expect(contentRoutes.filter(r=>r.startsWith("/charts/")&&r!=="/charts/")).toHaveLength(5)});it("includes a brand-selection chart hub",()=>{expect(contentRoutes).toContain("/charts/");const page=readFileSync("src/pages/charts/index.astro","utf8");expect(page).toContain("Choose your goalie pad brand");expect(page).toContain('/charts/${chart.slug}')} );it("excludes forbidden routes",()=>{expect(contentRoutes.some(r=>r.includes("vaughn"))).toBe(false)});it("keeps implementation proofs off the homepage",()=>{const page=readFileSync("src/pages/index.astro","utf8");expect(page).not.toMatch(/under construction|rules-engine proof|matched rule|component proof/i);expect(page).toContain('/charts/${chart.slug}')})});
